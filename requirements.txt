# MIRCrew Proxy - Dipendenze
# Web Framework
flask>=3.0.0

# HTTP client with Cloudflare TLS fingerprint bypass
curl_cffi>=0.7.0

# HTML Parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Cloudflare bypass via real browser (Chrome DevTools Protocol)
nodriver>=0.38
